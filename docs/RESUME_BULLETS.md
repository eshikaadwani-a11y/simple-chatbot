# Resume Bullets

Copy-paste options at three lengths. Pick based on space and how technical the
reader is. All are accurate to what's actually built; swap in a live demo link and
real metrics once deployed.

---

## 1-line version
> Built **LearnGraph**, a multi-agent AI learning platform (GPT-5, LangGraph,
> FastAPI, Next.js, Supabase) with tool-using agents, RAG-grounded answers,
> long-term memory, and streaming chat — deployed with CI/CD.

## 2-line version
> Built **LearnGraph**, a production-grade multi-agent AI tutor: a LangGraph
> supervisor routes requests to five specialized GPT-5 ReAct agents backed by 11
> tools, RAG retrieval with source citation, and dual-layer memory (Postgres
> checkpointer + Supabase).
> Shipped with human-in-the-loop approval, token-streaming APIs, JWT auth + rate
> limiting, LangSmith observability + cost analytics, and GitHub Actions CI/CD on
> Railway/Vercel.

## Advanced version
> Architected and built **LearnGraph**, a multi-agent AI learning platform on
> LangGraph + GPT-5: supervisor-based orchestration over five specialist agents,
> ReAct tool-calling (11 tools), and a **RAG pipeline** (OpenAI embeddings →
> pgvector retrieval → grounded, source-cited answers).
> Engineered for production: **human-in-the-loop** approval via graph interrupts
> with checkpoint-based resume, **dual-layer memory** (async Postgres checkpointer +
> relational long-term store), token-streaming SSE APIs, and a **model-agnostic**
> config layer (one-line swap between GPT-5/Claude/Gemini/DeepSeek).
> Hardened with Supabase JWT auth + RLS, per-IP rate limiting, secure headers, and
> input validation; instrumented with **LangSmith tracing and per-request token/cost
> analytics** surfaced in an admin dashboard; delivered with pytest/vitest suites, a
> 70% coverage gate, and GitHub Actions CI/CD deploying to Railway and Vercel.

---

## Skill keywords (for ATS)
LangGraph · GPT-5 · LLM agents · RAG · vector database (pgvector) · embeddings ·
FastAPI · async Python · Next.js · TypeScript · Supabase · PostgreSQL · Server-Sent
Events · JWT · rate limiting · Docker · Railway · Vercel · GitHub Actions · CI/CD ·
pytest · observability · LangSmith.

## Tips
- Lead with **impact + a number** once you have them (users, latency, cost/request).
- Add the **live demo link** — it's the single biggest credibility boost.
- For AI-engineering roles, foreground **RAG + multi-agent orchestration**; for
  backend roles, foreground **async FastAPI, Postgres, auth, rate limiting, CI/CD**.
